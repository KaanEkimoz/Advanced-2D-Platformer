namespace GlobalTypes
{
    public enum GroundType
    {
        None,
        DefaultPlatform,
        OneWayPlatform,
        MovingPlatform,
        CollapsablePlatform,
        JumpPad
    }
    public enum WallType
    {
        None,
        Normal,
        Sticky
    }

}